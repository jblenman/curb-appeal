# Anthropic API key — get one at https://console.anthropic.com
# Set a workspace-level $25/month spend cap.
ANTHROPIC_API_KEY=sk-ant-replace-me

# Model — Sonnet 4.6 is the default; Haiku is cheaper but worse at nuanced copy
ANTHROPIC_MODEL=claude-sonnet-4-6

# API server port (Vite proxies /api requests here)
API_PORT=8787
